#include <sys/socket.h>
#include <stdio.h>
#include <stdlib.h>
#include "socket.h"

int createSocket(){
    int sock 
    return sock;
}

void doBind(int sockfd,const struct sockaddr *addr, socklen_t addrlen){
    
}